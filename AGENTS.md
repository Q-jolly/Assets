# Development environment

- The development environment, including the Python environment and related tools, is located under `D:\QL\Environment`.
- Prefer the runtimes and dependencies from `D:\QL\Environment` when running project development or verification tasks.
